import { styled } from '@mui/system';
import { Card } from '@mui/material';

export const CardShadow = styled(Card)(() => ({
  boxShadow:
    '1px 3px 8px 0px rgb(0 0 0 / 20%), 0px 1px 1px 0px rgb(0 0 0 / 14%), 0px 1px 3px 0px rgb(0 0 0 / 12%)',
}));
